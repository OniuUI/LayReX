<Project>
  <PropertyGroup>
    <!-- CI on main: next patch on NuGet for this major.minor (0.3.*). Keep 0.3.0 here while shipping 0.3.1, 0.3.2, … Changing to 0.4.0 makes the next main publish 0.4.0, not 0.3.1. -->
    <LayeredChatVersion>0.4.0</LayeredChatVersion>
    <Version>$(LayeredChatVersion)</Version>
    <PackageVersion>$(LayeredChatVersion)</PackageVersion>
    <Authors>OniuUI</Authors>
    <PackageProjectUrl>https://github.com/OniuUI/LayReX</PackageProjectUrl>
    <RepositoryUrl>https://github.com/OniuUI/LayReX.git</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <ContinuousIntegrationBuild Condition="'$(GITHUB_ACTIONS)' == 'true'">true</ContinuousIntegrationBuild>
  </PropertyGroup>

  <ItemGroup>
    <None Include="$(MSBuildThisFileDirectory)README.md" Link="README.md" Pack="true" PackagePath="\" Visible="false" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="8.0.0" PrivateAssets="All" />
  </ItemGroup>
</Project>
